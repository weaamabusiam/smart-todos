<!DOCTYPE html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ניהול קטגוריות - מנהל משימות</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-content">
            <div class="navbar-brand">מנהל משימות - <%= username %></div>
            <div class="navbar-nav">
                <a href="/tasks">משימות</a>
                <a href="/categories">קטגוריות</a>
                <a href="/logout">התנתק</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="main-card">
            <h2>ניהול קטגוריות</h2>
            
            <% if (message) { %>
                <div class="message"><%= message %></div>
            <% } %>
            
            <form method="POST" action="/categories/new" style="margin-bottom: 30px;">
                <div style="display: flex; gap: 15px; align-items: center;">
                    <div class="form-group" style="flex: 1;">
                        <label for="name">שם הקטגוריה:</label>
                        <input type="text" id="name" name="name" required>
                    </div>
                    <div class="form-group">
                        <label for="color">צבע:</label>
                        <input type="color" id="color" name="color" value="#007bff" 
                               style="width: 60px; height: 44px; padding: 4px;">
                    </div>
                    <button type="submit" class="btn">הוסף קטגוריה</button>
                </div>
            </form>
            
            <% if (categories.length === 0) { %>
                <div style="text-align: center; padding: 40px; color: #666;">
                    <h3>אין קטגוריות עדיין</h3>
                    <p>צור קטגוריה ראשונה כדי להתחיל לנהל משימות</p>
                </div>
            <% } else { %>
                <% for (var i = 0; i < categories.length; i++) { %>
                    <% var category = categories[i]; %>
                    <div class="category-item">
                        <div class="category-info">
                            <span class="color-circle" style="background-color: <%= category.color %>;"></span>
                            <span class="category-name"><%= category.name %></span>
                        </div>
                        <div style="display: flex; gap: 10px;">
                            <a href="/categories/edit/<%= category.id %>" class="btn btn-small">ערוך</a>
                            <form method="POST" action="/categories/delete/<%= category.id %>" 
                                  onsubmit="return confirm('האם אתה בטוח שברצונך למחוק את הקטגוריה?')" 
                                  style="display: inline;">
                                <button type="submit" class="btn btn-danger btn-small">מחק</button>
                            </form>
                        </div>
                    </div>
                <% } %>
            <% } %>
        </div>
    </div>
</body>
</html> 