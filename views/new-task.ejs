<!DOCTYPE html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>משימה חדשה - מנהל משימות</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <div class="main-card">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px;">
                <h1>משימה חדשה</h1>
                <div>
                    <a href="/tasks" class="btn btn-small">חזור למשימות</a>
                    <a href="/logout" class="btn btn-small" style="background: #ff5252;">התנתק</a>
                </div>
            </div>
            
            <% if (error) { %>
                <div class="error"><%= error %></div>
            <% } %>
            
            <% if (message) { %>
                <div class="message"><%= message %></div>
            <% } %>
            
            <form method="POST" action="/tasks">
                <div class="form-group">
                    <label for="description">תיאור המשימה:</label>
                    <textarea id="description" name="description" required maxlength="200" placeholder="תאר את המשימה שלך..." style="min-height: 100px; resize: vertical;"></textarea>
                    <small style="color: #666; display: block; margin-top: 5px;">
                        <span id="charCount">0</span>/200 תווים
                    </small>
                </div>
                
                <div class="form-group">
                    <label for="target_date">תאריך יעד:</label>
                    <input type="date" id="target_date" name="target_date" required min="<%= new Date().toISOString().split('T')[0] %>">
                </div>
                
                <div class="form-group">
                    <label for="category_id">קטגוריה:</label>
                    <select id="category_id" name="category_id" required>
                        <option value="">בחר קטגוריה</option>
                        <% for (let i = 0; i < categories.length; i++) { %>
                            <option value="<%= categories[i].id %>" style="color: <%= categories[i].color %>;">
                                <%= categories[i].name %>
                            </option>
                        <% } %>
                    </select>
                </div>
                
                <div style="display: flex; gap: 15px;">
                    <button type="submit" class="btn">הוסף משימה</button>
                    <a href="/tasks" class="btn" style="background: #666;">ביטול</a>
                </div>
            </form>
        </div>
    </div>
    
    <script>
        // Character counter
        const textarea = document.getElementById('description');
        const charCount = document.getElementById('charCount');
        
        textarea.addEventListener('input', function() {
            charCount.textContent = this.value.length;
        });
    </script>
</body>
</html> 