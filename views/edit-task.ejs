<!DOCTYPE html>
<html lang="he">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>עריכת משימה - מנהל משימות</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-content">
            <div class="navbar-brand">מנהל משימות - <%= username %></div>
            <div class="navbar-nav">
                <a href="/tasks">משימות</a>
                <a href="/categories">קטגוריות</a>
                <a href="/logout">התנתק</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="main-card">
            <h2>עריכת משימה</h2>
            
            <% if (error) { %>
                <div class="error"><%= error %></div>
            <% } %>
            
            <form method="POST" action="/tasks/edit/<%= task.id %>">
                <div class="form-group">
                    <label for="description">תיאור המשימה:</label>
                    <textarea id="description" name="description" required maxlength="200" rows="3"><%= task.description %></textarea>
                </div>
                
                <div class="form-group">
                    <label for="target_date">תאריך יעד:</label>
                    <input type="date" id="target_date" name="target_date" value="<%= task.target_date %>" required>
                </div>
                
                <div class="form-group">
                    <label for="category_id">קטגוריה:</label>
                    <select id="category_id" name="category_id" required>
                        <% for (var i = 0; i < categories.length; i++) { %>
                            <option value="<%= categories[i].id %>" 
                                    <% if (task.category_id == categories[i].id) { %>selected<% } %>>
                                <%= categories[i].name %>
                            </option>
                        <% } %>
                    </select>
                </div>
                
                <div style="display: flex; gap: 15px; margin-top: 20px;">
                    <button type="submit" class="btn btn-success">שמור שינויים</button>
                    <a href="/tasks" class="btn" style="text-decoration: none;">ביטול</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html> 